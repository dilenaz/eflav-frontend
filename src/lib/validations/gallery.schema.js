import{z}from'zod';import{IMAGE_URL_MESSAGE,isAllowedImageUrl}from'@/lib/image-url';export const gallerySchema=z.object({title:z.string().trim().min(3).max(255),slug:z.string().trim().min(3).max(255).regex(/^[a-z0-9]+(?:-[a-z0-9]+)*$/),summary:z.string().trim().min(10).max(500),content:z.string().trim().min(20).max(100000),coverImageUrl:z.string().trim().max(500).refine(isAllowedImageUrl,IMAGE_URL_MESSAGE).nullable().optional(),coverImageAlt:z.string().trim().max(255).nullable().optional(),status:z.enum(['draft','published','archived']),publishedAt:z.string().datetime().nullable().optional(),images:z.array(z.object({url:z.string().trim().min(1).max(500).refine(isAllowedImageUrl,IMAGE_URL_MESSAGE),alt:z.string().trim().min(2).max(255)})).max(50).default([])});
